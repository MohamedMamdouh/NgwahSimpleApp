import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 40,
    alignItems: 'center',
    justifyContent: 'center',
    width: 300
  },
  textInput: {
    width: '100%',
    borderColor: '#000',
    borderWidth: 1,
    height: 30,
    paddingLeft: 10,
    borderRadius: 3
  }
});
